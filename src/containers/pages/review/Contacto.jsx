import { useEffect, useState } from "react";
import Layout from "../../../layout/layout";


const Contacto = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);


  return (
    <Layout>
      
    </Layout>
  );
};

export default Contacto;
